trigger AccountTrigger02 on Account(after insert) {
    /*//建立一个新的订单对象
    Order ord = new Order();
    for(Account acc : Trigger.new){
        ord.AccountId = acc.Id;
        ord.Name = '订单B';
        //ord.EffectiveDate = date.today();
        //ord.Status = 'Draft';

    }
    insert ord;*/
}